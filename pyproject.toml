[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["F", "E", "W", "I"]

[tool.pytest.ini_options]
addopts = "--cov=app --cov-report=term-missing"

[tool.coverage.run]
omit = [
    "app/main.py",
    "app/core/config.py"
]